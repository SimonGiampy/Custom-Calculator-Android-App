apply plugin: 'com.android.application'

android {
    signingConfigs {
        releaseReadyConfig {
            keyAlias 'SGkeyStore'
            keyPassword 'i<3devAPPS'
            storeFile file('D:/Android Studio/keyStore.jks')
            storePassword 'i<3devAPPS'
        }
    }
    compileSdkVersion 28
    defaultConfig {
        applicationId "giampy.simon.customcalculator"
        minSdkVersion 14
        targetSdkVersion 28
        versionCode 2
        versionName "1.1"
        signingConfig signingConfigs.releaseReadyConfig
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.releaseReadyConfig
        }
    }
    productFlavors {
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation 'com.android.support:appcompat-v7:23.4.0'
    implementation 'com.udojava:EvalEx:1.1'
    implementation 'com.android.support:recyclerview-v7:23.4.0'
    implementation 'com.android.support:design:23.4.0'
    implementation 'com.melnykov:floatingactionbutton:1.3.0'
}
